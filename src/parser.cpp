/*
 */

#include "../include/parser.h"

using namespace std;

std::list<Border> parseFile(const char * path)
{
    fstream file(path);
    
    
    
    file.close();
}

Border parseLine(const stringstream & str)
{
    
}
